// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Belt_Array from "rescript/lib/es6/Belt_Array.js";
import * as Stdlib_List from "rescript/lib/es6/Stdlib_List.js";
import * as Stdlib_Array from "rescript/lib/es6/Stdlib_Array.js";
import * as Primitive_int from "rescript/lib/es6/Primitive_int.js";
import * as Stdlib_Option from "rescript/lib/es6/Stdlib_Option.js";
import * as TableclothMap from "./TableclothMap.res.mjs";
import * as Stdlib_Ordering from "rescript/lib/es6/Stdlib_Ordering.js";
import * as Primitive_option from "rescript/lib/es6/Primitive_option.js";

function singleton(a) {
  return [a];
}

function clone(t) {
  return t.map(prim => prim);
}

function isEmpty(a) {
  return a.length === 0;
}

let initialize = Stdlib_Array.fromInitializer;

function range(fromOpt, to_) {
  let from = fromOpt !== undefined ? fromOpt : 0;
  return Stdlib_Array.fromInitializer(to_ - from | 0, i => i + from | 0);
}

function toIndexedList(array) {
  return Stdlib_Array.reduceRight(array, [
      array.length - 1 | 0,
      /* [] */0
    ], (param, x) => {
      let i = param[0];
      return [
        i - 1 | 0,
        {
          hd: [
            i,
            x
          ],
          tl: param[1]
        }
      ];
    })[1];
}

function getUnsafe(prim0, prim1) {
  return prim0[prim1];
}

function first(__x) {
  return __x[0];
}

function setUnsafe(prim0, prim1, prim2) {
  prim0[prim1] = prim2;
}

function swap(t, i, j) {
  let temp = Stdlib_Option.getExn(t[i], undefined);
  t[i] = Stdlib_Option.getExn(t[j], undefined);
  t[j] = temp;
}

function fold(t, initial, f) {
  return Stdlib_Array.reduce(t, initial, (a, b) => f(a, b));
}

function foldRight(t, initial, f) {
  return Stdlib_Array.reduceRight(t, initial, (a, b) => f(a, b));
}

function maximum(t, compare) {
  return fold(t, undefined, (max, element) => {
    if (max !== undefined && !Stdlib_Ordering.isGreater(compare(element, Primitive_option.valFromOption(max)))) {
      return max;
    } else {
      return Primitive_option.some(element);
    }
  });
}

function minimum(t, compare) {
  return fold(t, undefined, (min, element) => {
    if (min !== undefined && !Stdlib_Ordering.isLess(compare(element, Primitive_option.valFromOption(min)))) {
      return min;
    } else {
      return Primitive_option.some(element);
    }
  });
}

function extent(t, compare) {
  return fold(t, undefined, (range, element) => {
    if (range === undefined) {
      return [
        element,
        element
      ];
    }
    let max = range[1];
    let min = range[0];
    return [
      Stdlib_Ordering.isLess(compare(element, min)) ? element : min,
      Stdlib_Ordering.isGreater(compare(element, max)) ? element : max
    ];
  });
}

function sum(t, M) {
  return Stdlib_Array.reduce(t, M.zero, M.add);
}

let map2 = Belt_Array.zipBy;

function map3(as_, bs, cs, f) {
  let minLength = Stdlib_Array.reduce([
    bs.length,
    cs.length
  ], as_.length, Primitive_int.min);
  return Stdlib_Array.fromInitializer(minLength, i => f(Stdlib_Option.getExn(as_[i], undefined), Stdlib_Option.getExn(bs[i], undefined), Stdlib_Option.getExn(cs[i], undefined)));
}

function zip(a, b) {
  return Belt_Array.zipBy(a, b, (a, b) => [
    a,
    b
  ]);
}

function sliding(stepOpt, a, size) {
  let step = stepOpt !== undefined ? stepOpt : 1;
  let n = a.length;
  if (size > n) {
    return [];
  } else {
    return Stdlib_Array.fromInitializer(1 + Primitive_int.div(n - size | 0, step) | 0, i => Stdlib_Array.fromInitializer(size, j => Stdlib_Option.getExn(a[Math.imul(i, step) + j | 0], undefined)));
  }
}

function findIndex(array, f) {
  let _index = 0;
  while (true) {
    let index = _index;
    if (index >= array.length) {
      return;
    }
    if (f(index, Stdlib_Option.getExn(array[index], undefined))) {
      return [
        index,
        Stdlib_Option.getExn(array[index], undefined)
      ];
    }
    _index = index + 1 | 0;
    continue;
  };
}

function any(prim0, prim1) {
  return prim0.some(prim1);
}

function all(prim0, prim1) {
  return prim0.every(prim1);
}

function includes(t, v, equal) {
  return t.some(__x => equal(v, __x));
}

function append(prim0, prim1) {
  return prim0.concat(prim1);
}

function flatten(prim) {
  return prim.flat();
}

function intersperse(t, sep) {
  return Stdlib_Array.fromInitializer(Primitive_int.max(0, (t.length << 1) - 1 | 0), i => {
    if (i % 2 !== 0) {
      return sep;
    } else {
      return Stdlib_Option.getExn(t[i / 2 | 0], undefined);
    }
  });
}

function count(t, f) {
  return fold(t, 0, (total, element) => total + (
    f(element) ? 1 : 0
  ) | 0);
}

function chunksOf(t, size) {
  return sliding(size, t, size);
}

function partition(t, f) {
  let match = foldRight(t, [
    /* [] */0,
    /* [] */0
  ], (param, element) => {
    let rights = param[1];
    let lefts = param[0];
    if (f(element)) {
      return [
        {
          hd: element,
          tl: lefts
        },
        rights
      ];
    } else {
      return [
        lefts,
        {
          hd: element,
          tl: rights
        }
      ];
    }
  });
  return [
    Stdlib_List.toArray(match[0]),
    Stdlib_List.toArray(match[1])
  ];
}

function splitAt(t, index) {
  return [
    t.slice(0, index),
    t.slice(index, t.length)
  ];
}

function splitWhen(t, f) {
  let match = findIndex(t, (param, e) => f(e));
  if (match !== undefined) {
    return splitAt(t, match[0]);
  } else {
    return [
      t,
      []
    ];
  }
}

function unzip(t) {
  return [
    Stdlib_Array.fromInitializer(t.length, i => Stdlib_Option.getExn(t[i], undefined)[0]),
    Stdlib_Array.fromInitializer(t.length, i => Stdlib_Option.getExn(t[i], undefined)[1])
  ];
}

function repeat(element, length) {
  return Stdlib_Array.fromInitializer(Primitive_int.max(length, 0), param => element);
}

function groupBy(t, comparator, f) {
  return fold(t, TableclothMap.empty(comparator), (map, element) => {
    let key = f(element);
    return TableclothMap.update(map, key, x => {
      if (x !== undefined) {
        return {
          hd: element,
          tl: x
        };
      } else {
        return {
          hd: element,
          tl: /* [] */0
        };
      }
    });
  });
}

let filterMap = Stdlib_Array.filterMap;

let values = Stdlib_Array.keepSome;

let equal = Stdlib_Array.equal;

export {
  singleton,
  repeat,
  range,
  initialize,
  clone,
  getUnsafe,
  setUnsafe,
  first,
  swap,
  isEmpty,
  any,
  all,
  count,
  findIndex,
  includes,
  minimum,
  maximum,
  extent,
  sum,
  filterMap,
  fold,
  foldRight,
  append,
  flatten,
  zip,
  map2,
  map3,
  partition,
  splitAt,
  splitWhen,
  unzip,
  values,
  intersperse,
  chunksOf,
  sliding,
  groupBy,
  toIndexedList,
  equal,
}
/* TableclothMap Not a pure module */
