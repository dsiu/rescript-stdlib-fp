// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Belt_List from "@rescript/runtime/lib/es6/Belt_List.js";
import * as Stdlib_List from "@rescript/runtime/lib/es6/Stdlib_List.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as TableclothMap from "./TableclothMap.res.mjs";
import * as Belt_SetString from "@rescript/runtime/lib/es6/Belt_SetString.js";
import * as Stdlib_Ordering from "@rescript/runtime/lib/es6/Stdlib_Ordering.js";
import * as Primitive_object from "@rescript/runtime/lib/es6/Primitive_object.js";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";

function singleton(x) {
  return {
    hd: x,
    tl: /* [] */0
  };
}

function range(fromOpt, to_) {
  let from = fromOpt !== undefined ? fromOpt : 0;
  if (to_ < from) {
    return /* [] */0;
  } else {
    return Stdlib_List.fromInitializer(to_ - from | 0, i => i + from | 0);
  }
}

function repeat(element, times) {
  if (times <= 0) {
    return /* [] */0;
  } else {
    return {
      hd: element,
      tl: repeat(element, times - 1 | 0)
    };
  }
}

function sum(t, M) {
  return Stdlib_List.reduce(t, M.zero, M.add);
}

function flatMap(t, f) {
  return Stdlib_List.flat(Stdlib_List.map(t, f));
}

let map2 = Stdlib_List.zipBy;

function map3(a, b, c, f) {
  if (a !== 0 && b !== 0 && c !== 0) {
    return {
      hd: f(a.hd, b.hd, c.hd),
      tl: map3(a.tl, b.tl, c.tl, f)
    };
  } else {
    return /* [] */0;
  }
}

function last(_l) {
  while (true) {
    let l = _l;
    if (l === 0) {
      return;
    }
    let rest = l.tl;
    if (rest === 0) {
      return Primitive_option.some(l.hd);
    }
    _l = rest;
    continue;
  };
}

function uniqueBy(l, f) {
  let _existing;
  let _remaining = l;
  let _accumulator = /* [] */0;
  while (true) {
    let accumulator = _accumulator;
    let remaining = _remaining;
    let existing = _existing;
    if (remaining === 0) {
      return Stdlib_List.reverse(accumulator);
    }
    let rest = remaining.tl;
    let first = remaining.hd;
    let computedFirst = f(first);
    if (Belt_SetString.has(existing, computedFirst)) {
      _remaining = rest;
      continue;
    }
    _accumulator = {
      hd: first,
      tl: accumulator
    };
    _remaining = rest;
    _existing = Belt_SetString.add(existing, computedFirst);
    continue;
  };
}

let getAt = Belt_List.get;

function any(t, f) {
  return Stdlib_Option.isSome(Stdlib_List.find(t, f));
}

function initial(l) {
  let match = Stdlib_List.reverse(l);
  if (match !== 0) {
    return Stdlib_List.reverse(match.tl);
  }
  
}

function fold(t, initial, f) {
  return Stdlib_List.reduce(t, initial, (a, b) => f(a, b));
}

function count(t, f) {
  return fold(t, 0, (total, element) => total + (
    f(element) ? 1 : 0
  ) | 0);
}

function foldRight(t, initial, f) {
  return Belt_List.reduceReverse(t, initial, (a, b) => f(a, b));
}

function findIndex(list, f) {
  let _i = 0;
  let _l = list;
  while (true) {
    let l = _l;
    let i = _i;
    if (l === 0) {
      return;
    }
    let x = l.hd;
    if (f(i, x)) {
      return [
        i,
        x
      ];
    }
    _l = l.tl;
    _i = i + 1 | 0;
    continue;
  };
}

function updateAt(t, index, f) {
  return Belt_List.mapWithIndex(t, (i, element) => {
    if (i === index) {
      return f(element);
    } else {
      return element;
    }
  });
}

function dropWhile(_t, f) {
  while (true) {
    let t = _t;
    if (t === 0) {
      return /* [] */0;
    }
    if (!f(t.hd)) {
      return t;
    }
    _t = t.tl;
    continue;
  };
}

function isEmpty(t) {
  return t === /* [] */0;
}

function sliding(stepOpt, t, size) {
  let step = stepOpt !== undefined ? stepOpt : 1;
  let loop = t => {
    if (t === /* [] */0) {
      return /* [] */0;
    }
    let sample = Belt_List.take(t, size);
    let rest = Belt_List.drop(t, step);
    if (sample !== undefined) {
      if (rest !== undefined) {
        return {
          hd: sample,
          tl: loop(rest)
        };
      } else {
        return {
          hd: sample,
          tl: /* [] */0
        };
      }
    } else {
      return /* [] */0;
    }
  };
  return loop(t);
}

function chunksOf(t, size) {
  return sliding(size, t, size);
}

function cons(t, element) {
  return {
    hd: element,
    tl: t
  };
}

function takeWhile(t, f) {
  let _acc = /* [] */0;
  let _t = t;
  while (true) {
    let t$1 = _t;
    let acc = _acc;
    if (t$1 === 0) {
      return Stdlib_List.reverse(acc);
    }
    let x = t$1.hd;
    if (!f(x)) {
      return Stdlib_List.reverse(acc);
    }
    _t = t$1.tl;
    _acc = {
      hd: x,
      tl: acc
    };
    continue;
  };
}

function removeAt(t, index) {
  if (index < 0) {
    return t;
  }
  let match = Stdlib_List.splitAt(t, index);
  if (match === undefined) {
    return t;
  }
  let t$1 = Stdlib_List.tail(match[1]);
  if (t$1 !== undefined) {
    return Stdlib_List.concat(match[0], t$1);
  } else {
    return t;
  }
}

function minimumBy(l, f) {
  let minBy = (param, x) => {
    let fy = param[1];
    let fx = f(x);
    if (Primitive_object.lessthan(fx, fy)) {
      return [
        x,
        fx
      ];
    } else {
      return [
        param[0],
        fy
      ];
    }
  };
  if (l === 0) {
    return;
  }
  let rest = l.tl;
  let x = l.hd;
  if (rest !== 0) {
    return Primitive_option.some(fold(rest, [
        x,
        f(x)
      ], minBy)[0]);
  } else {
    return Primitive_option.some(x);
  }
}

function maximumBy(l, f) {
  let maxBy = (param, x) => {
    let fy = param[1];
    let fx = f(x);
    if (Primitive_object.greaterthan(fx, fy)) {
      return [
        x,
        fx
      ];
    } else {
      return [
        param[0],
        fy
      ];
    }
  };
  if (l === 0) {
    return;
  }
  let rest = l.tl;
  let x = l.hd;
  if (rest !== 0) {
    return Primitive_option.some(fold(rest, [
        x,
        f(x)
      ], maxBy)[0]);
  } else {
    return Primitive_option.some(x);
  }
}

function minimum(t, compare) {
  return fold(t, undefined, (min, element) => {
    if (min !== undefined && !Stdlib_Ordering.isLess(compare(element, Primitive_option.valFromOption(min)))) {
      return min;
    } else {
      return Primitive_option.some(element);
    }
  });
}

function maximum(t, compare) {
  return fold(t, undefined, (max, element) => {
    if (max !== undefined && !Stdlib_Ordering.isGreater(compare(element, Primitive_option.valFromOption(max)))) {
      return max;
    } else {
      return Primitive_option.some(element);
    }
  });
}

function extent(t, compare) {
  return fold(t, undefined, (current, element) => {
    if (current === undefined) {
      return [
        element,
        element
      ];
    }
    let max = current[1];
    let min = current[0];
    return [
      Stdlib_Ordering.isLess(compare(element, min)) ? element : min,
      Stdlib_Ordering.isGreater(compare(element, max)) ? element : max
    ];
  });
}

function sortBy(l, f) {
  return Belt_List.sort(l, (a, b) => {
    let a$p = f(a);
    let b$p = f(b);
    if (Primitive_object.equal(a$p, b$p)) {
      return 0;
    } else if (Primitive_object.lessthan(a$p, b$p)) {
      return -1;
    } else {
      return 1;
    }
  });
}

function groupWhile(l, f) {
  let $$break = (param, x, y) => f(x, y);
  let groups = Belt_List.reduceWithIndex(l, /* [] */0, (acc, x, i) => {
    if (acc === 0) {
      return {
        hd: {
          hd: x,
          tl: /* [] */0
        },
        tl: /* [] */0
      };
    }
    let tl = acc.tl;
    let current_group = acc.hd;
    if ($$break(i, Belt_List.headExn(current_group), x)) {
      return {
        hd: {
          hd: x,
          tl: /* [] */0
        },
        tl: {
          hd: current_group,
          tl: tl
        }
      };
    } else {
      return {
        hd: {
          hd: x,
          tl: current_group
        },
        tl: tl
      };
    }
  });
  if (groups !== 0) {
    return Belt_List.mapReverse(groups, Stdlib_List.reverse);
  } else {
    return /* [] */0;
  }
}

function insertAt(t, index, value) {
  let match = Stdlib_List.splitAt(t, index);
  if (match !== undefined) {
    return Stdlib_List.concat(match[0], {
      hd: value,
      tl: match[1]
    });
  } else {
    return t;
  }
}

function splitWhen(t, f) {
  let _front = /* [] */0;
  let _back = t;
  while (true) {
    let back = _back;
    let front = _front;
    if (back === 0) {
      return [
        t,
        /* [] */0
      ];
    }
    let element = back.hd;
    if (f(element)) {
      return [
        Stdlib_List.reverse(front),
        back
      ];
    }
    _back = back.tl;
    _front = {
      hd: element,
      tl: front
    };
    continue;
  };
}

function intersperse(t, sep) {
  if (t === 0) {
    return /* [] */0;
  }
  let rest = t.tl;
  let x = t.hd;
  if (rest !== 0) {
    return {
      hd: x,
      tl: foldRight(rest, /* [] */0, (acc, x) => ({
        hd: sep,
        tl: {
          hd: x,
          tl: acc
        }
      }))
    };
  } else {
    return {
      hd: x,
      tl: /* [] */0
    };
  }
}

function initialize(length, f) {
  return Stdlib_List.fromInitializer(length, a => f(a));
}

function join(strings, sep) {
  return Stdlib_List.toArray(strings).join(sep);
}

function groupBy(t, comparator, f) {
  return fold(t, TableclothMap.empty(comparator), (map, element) => {
    let key = f(element);
    return TableclothMap.update(map, key, x => {
      if (x !== undefined) {
        return {
          hd: element,
          tl: x
        };
      } else {
        return {
          hd: element,
          tl: /* [] */0
        };
      }
    });
  });
}

let empty = /* [] */0;

let take = Stdlib_List.take;

let drop = Stdlib_List.drop;

let all = Stdlib_List.every;

let find = Stdlib_List.find;

let includes = Stdlib_List.has;

let append = Stdlib_List.concat;

export {
  empty,
  singleton,
  repeat,
  range,
  initialize,
  cons,
  take,
  takeWhile,
  drop,
  dropWhile,
  initial,
  last,
  getAt,
  insertAt,
  updateAt,
  removeAt,
  sortBy,
  isEmpty,
  any,
  all,
  count,
  uniqueBy,
  find,
  findIndex,
  includes,
  minimumBy,
  maximumBy,
  minimum,
  maximum,
  extent,
  sum,
  flatMap,
  fold,
  foldRight,
  append,
  map2,
  map3,
  splitWhen,
  intersperse,
  chunksOf,
  sliding,
  groupWhile,
  join,
  groupBy,
}
/* TableclothMap Not a pure module */
