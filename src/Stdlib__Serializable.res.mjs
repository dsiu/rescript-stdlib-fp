// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Int from "@rescript/runtime/lib/es6/Stdlib_Int.js";
import * as Stdlib_JSON from "@rescript/runtime/lib/es6/Stdlib_JSON.js";
import * as Stdlib_Float from "@rescript/runtime/lib/es6/Stdlib_Float.js";
import * as Stdlib_BigInt from "@rescript/runtime/lib/es6/Stdlib_BigInt.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";

function MakeArray(A) {
  let toString = arr => JSON.stringify(arr.map(A.jsonEncode));
  let fromString = str => Stdlib_Option.flatMap(Stdlib_JSON.Decode.array(JSON.parse(str)), arr => arr.map(x => Stdlib_Option.getExn(A.jsonDecode(x), undefined)));
  let fromStringUnsafe = str => Stdlib_Option.getExn(fromString(str), undefined);
  return {
    toString: toString,
    fromString: fromString,
    fromStringUnsafe: fromStringUnsafe
  };
}

function MakeTuple2(A) {
  return B => {
    let toString = param => JSON.stringify([
      A.jsonEncode(param[0]),
      B.jsonEncode(param[1])
    ]);
    let fromString = str => {
      let arr = Stdlib_JSON.Decode.array(JSON.parse(str));
      let a = Stdlib_Option.flatMap(arr, arr => Stdlib_Option.flatMap(arr[0], A.jsonDecode));
      let b = Stdlib_Option.flatMap(arr, arr => Stdlib_Option.flatMap(arr[1], B.jsonDecode));
      if (a !== undefined && b !== undefined) {
        return [
          Primitive_option.valFromOption(a),
          Primitive_option.valFromOption(b)
        ];
      }
      
    };
    let fromStringUnsafe = str => Stdlib_Option.getExn(fromString(str), undefined);
    return {
      toString: toString,
      fromString: fromString,
      fromStringUnsafe: fromStringUnsafe
    };
  };
}

function MakeTuple3(A) {
  return B => (C => {
    let toString = param => JSON.stringify([
      A.jsonEncode(param[0]),
      B.jsonEncode(param[1]),
      C.jsonEncode(param[2])
    ]);
    let fromString = str => {
      let arr = Stdlib_JSON.Decode.array(JSON.parse(str));
      let a = Stdlib_Option.flatMap(arr, arr => Stdlib_Option.flatMap(arr[0], A.jsonDecode));
      let b = Stdlib_Option.flatMap(arr, arr => Stdlib_Option.flatMap(arr[1], B.jsonDecode));
      let c = Stdlib_Option.flatMap(arr, arr => Stdlib_Option.flatMap(arr[2], C.jsonDecode));
      if (a !== undefined && b !== undefined && c !== undefined) {
        return [
          Primitive_option.valFromOption(a),
          Primitive_option.valFromOption(b),
          Primitive_option.valFromOption(c)
        ];
      }
      
    };
    let fromStringUnsafe = str => Stdlib_Option.getExn(fromString(str), undefined);
    return {
      toString: toString,
      fromString: fromString,
      fromStringUnsafe: fromStringUnsafe
    };
  });
}

function toString(s) {
  return s;
}

function fromString(s) {
  return Primitive_option.some(s);
}

function fromStringUnsafe(s) {
  return s;
}

let $$String = {
  toString: toString,
  fromString: fromString,
  fromStringUnsafe: fromStringUnsafe
};

function toString$1(prim0, prim1) {
  return prim0.toString(prim1 !== undefined ? Primitive_option.valFromOption(prim1) : undefined);
}

function fromStringUnsafe$1(s) {
  return Stdlib_Option.getExn(Stdlib_Int.fromString(s, undefined), undefined);
}

let Int = {
  toString: toString$1,
  fromString: Stdlib_Int.fromString,
  fromStringUnsafe: fromStringUnsafe$1
};

function toString$2(prim0, prim1) {
  return prim0.toString(prim1 !== undefined ? Primitive_option.valFromOption(prim1) : undefined);
}

function fromStringUnsafe$2(s) {
  return Stdlib_Option.getExn(Stdlib_Float.fromString(s), undefined);
}

let Float = {
  toString: toString$2,
  fromString: Stdlib_Float.fromString,
  fromStringUnsafe: fromStringUnsafe$2
};

function toString$3(prim0, prim1) {
  return prim0.toString(prim1 !== undefined ? Primitive_option.valFromOption(prim1) : undefined);
}

function fromString$1(prim) {
  return BigInt(prim);
}

let fromStringUnsafe$3 = Stdlib_BigInt.fromString;

let $$BigInt$1 = {
  toString: toString$3,
  fromString: fromString$1,
  fromStringUnsafe: fromStringUnsafe$3
};

let JSONSerializable;

export {
  JSONSerializable,
  MakeArray,
  MakeTuple2,
  MakeTuple3,
  $$String,
  Int,
  Float,
  $$BigInt$1 as $$BigInt,
}
/* No side effect */
