// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Option from "rescript/lib/es6/Stdlib_Option.js";
import * as Stdlib__Array from "./Stdlib__Array.res.mjs";
import * as Stdlib__Serializable from "./Stdlib__Serializable.res.mjs";
import * as Stdlib__JSONSerializable from "./Stdlib__JSONSerializable.res.mjs";

function Make(Serializable) {
  let make = prim => new Map();
  let fromArray = arr => new Map(arr.map(param => [
    Serializable.toString(param[0]),
    param[1]
  ]));
  let fromIterator = iter => fromArray(Array.from(iter));
  let size = prim => prim.size;
  let clear = prim => {
    prim.clear();
  };
  let forEach = (prim0, prim1) => {
    prim0.forEach(prim1);
  };
  let forEachWithKey = (t, f) => {
    t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable.fromString(k), undefined)));
  };
  let get = (t, k) => t.get(Serializable.toString(k));
  let has = (t, k) => t.has(Serializable.toString(k));
  let set = (t, k, v) => {
    t.set(Serializable.toString(k), v);
  };
  let $$delete = (t, k) => t.delete(Serializable.toString(k));
  let keys = t => Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable.fromString(x), undefined)));
  let values = prim => prim.values();
  let entries = t => Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
    Stdlib_Option.getExn(Serializable.fromString(param[0]), undefined),
    param[1]
  ]));
  return {
    make: make,
    fromArray: fromArray,
    fromIterator: fromIterator,
    size: size,
    clear: clear,
    forEach: forEach,
    forEachWithKey: forEachWithKey,
    get: get,
    has: has,
    set: set,
    $$delete: $$delete,
    keys: keys,
    values: values,
    entries: entries
  };
}

function Make$1(A) {
  let Serializable = Stdlib__Serializable.MakeArray(A);
  let make = prim => new Map();
  let fromArray = arr => new Map(arr.map(param => [
    Serializable.toString(param[0]),
    param[1]
  ]));
  let fromIterator = iter => fromArray(Array.from(iter));
  let size = prim => prim.size;
  let clear = prim => {
    prim.clear();
  };
  let forEach = (prim0, prim1) => {
    prim0.forEach(prim1);
  };
  let forEachWithKey = (t, f) => {
    t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable.fromString(k), undefined)));
  };
  let get = (t, k) => t.get(Serializable.toString(k));
  let has = (t, k) => t.has(Serializable.toString(k));
  let set = (t, k, v) => {
    t.set(Serializable.toString(k), v);
  };
  let $$delete = (t, k) => t.delete(Serializable.toString(k));
  let keys = t => Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable.fromString(x), undefined)));
  let values = prim => prim.values();
  let entries = t => Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
    Stdlib_Option.getExn(Serializable.fromString(param[0]), undefined),
    param[1]
  ]));
  return {
    make: make,
    fromArray: fromArray,
    fromIterator: fromIterator,
    size: size,
    clear: clear,
    forEach: forEach,
    forEachWithKey: forEachWithKey,
    get: get,
    has: has,
    set: set,
    $$delete: $$delete,
    keys: keys,
    values: values,
    entries: entries
  };
}

let Serializable = Stdlib__Serializable.MakeArray(Stdlib__JSONSerializable.Int);

function make(prim) {
  return new Map();
}

function fromArray(arr) {
  return new Map(arr.map(param => [
    Serializable.toString(param[0]),
    param[1]
  ]));
}

function fromIterator(iter) {
  return fromArray(Array.from(iter));
}

function size(prim) {
  return prim.size;
}

function clear(prim) {
  prim.clear();
}

function forEach(prim0, prim1) {
  prim0.forEach(prim1);
}

function forEachWithKey(t, f) {
  t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable.fromString(k), undefined)));
}

function get(t, k) {
  return t.get(Serializable.toString(k));
}

function has(t, k) {
  return t.has(Serializable.toString(k));
}

function set(t, k, v) {
  t.set(Serializable.toString(k), v);
}

function $$delete(t, k) {
  return t.delete(Serializable.toString(k));
}

function keys(t) {
  return Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable.fromString(x), undefined)));
}

function values(prim) {
  return prim.values();
}

function entries(t) {
  return Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
    Stdlib_Option.getExn(Serializable.fromString(param[0]), undefined),
    param[1]
  ]));
}

let Int = {
  make: make,
  fromArray: fromArray,
  fromIterator: fromIterator,
  size: size,
  clear: clear,
  forEach: forEach,
  forEachWithKey: forEachWithKey,
  get: get,
  has: has,
  set: set,
  $$delete: $$delete,
  keys: keys,
  values: values,
  entries: entries
};

let Serializable$1 = Stdlib__Serializable.MakeArray(Stdlib__JSONSerializable.$$String);

function make$1(prim) {
  return new Map();
}

function fromArray$1(arr) {
  return new Map(arr.map(param => [
    Serializable$1.toString(param[0]),
    param[1]
  ]));
}

function fromIterator$1(iter) {
  return fromArray$1(Array.from(iter));
}

function size$1(prim) {
  return prim.size;
}

function clear$1(prim) {
  prim.clear();
}

function forEach$1(prim0, prim1) {
  prim0.forEach(prim1);
}

function forEachWithKey$1(t, f) {
  t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable$1.fromString(k), undefined)));
}

function get$1(t, k) {
  return t.get(Serializable$1.toString(k));
}

function has$1(t, k) {
  return t.has(Serializable$1.toString(k));
}

function set$1(t, k, v) {
  t.set(Serializable$1.toString(k), v);
}

function $$delete$1(t, k) {
  return t.delete(Serializable$1.toString(k));
}

function keys$1(t) {
  return Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable$1.fromString(x), undefined)));
}

function values$1(prim) {
  return prim.values();
}

function entries$1(t) {
  return Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
    Stdlib_Option.getExn(Serializable$1.fromString(param[0]), undefined),
    param[1]
  ]));
}

let $$String = {
  make: make$1,
  fromArray: fromArray$1,
  fromIterator: fromIterator$1,
  size: size$1,
  clear: clear$1,
  forEach: forEach$1,
  forEachWithKey: forEachWithKey$1,
  get: get$1,
  has: has$1,
  set: set$1,
  $$delete: $$delete$1,
  keys: keys$1,
  values: values$1,
  entries: entries$1
};

let Serializable$2 = Stdlib__Serializable.MakeArray(Stdlib__JSONSerializable.Float);

function make$2(prim) {
  return new Map();
}

function fromArray$2(arr) {
  return new Map(arr.map(param => [
    Serializable$2.toString(param[0]),
    param[1]
  ]));
}

function fromIterator$2(iter) {
  return fromArray$2(Array.from(iter));
}

function size$2(prim) {
  return prim.size;
}

function clear$2(prim) {
  prim.clear();
}

function forEach$2(prim0, prim1) {
  prim0.forEach(prim1);
}

function forEachWithKey$2(t, f) {
  t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable$2.fromString(k), undefined)));
}

function get$2(t, k) {
  return t.get(Serializable$2.toString(k));
}

function has$2(t, k) {
  return t.has(Serializable$2.toString(k));
}

function set$2(t, k, v) {
  t.set(Serializable$2.toString(k), v);
}

function $$delete$2(t, k) {
  return t.delete(Serializable$2.toString(k));
}

function keys$2(t) {
  return Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable$2.fromString(x), undefined)));
}

function values$2(prim) {
  return prim.values();
}

function entries$2(t) {
  return Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
    Stdlib_Option.getExn(Serializable$2.fromString(param[0]), undefined),
    param[1]
  ]));
}

let Float = {
  make: make$2,
  fromArray: fromArray$2,
  fromIterator: fromIterator$2,
  size: size$2,
  clear: clear$2,
  forEach: forEach$2,
  forEachWithKey: forEachWithKey$2,
  get: get$2,
  has: has$2,
  set: set$2,
  $$delete: $$delete$2,
  keys: keys$2,
  values: values$2,
  entries: entries$2
};

let $$Array = {
  Make: Make$1,
  Int: Int,
  $$String: $$String,
  Float: Float
};

function Make$2(A) {
  return B => {
    let Serializable = Stdlib__Serializable.MakeTuple2(A)(B);
    let make = prim => new Map();
    let fromArray = arr => new Map(arr.map(param => [
      Serializable.toString(param[0]),
      param[1]
    ]));
    let fromIterator = iter => fromArray(Array.from(iter));
    let size = prim => prim.size;
    let clear = prim => {
      prim.clear();
    };
    let forEach = (prim0, prim1) => {
      prim0.forEach(prim1);
    };
    let forEachWithKey = (t, f) => {
      t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable.fromString(k), undefined)));
    };
    let get = (t, k) => t.get(Serializable.toString(k));
    let has = (t, k) => t.has(Serializable.toString(k));
    let set = (t, k, v) => {
      t.set(Serializable.toString(k), v);
    };
    let $$delete = (t, k) => t.delete(Serializable.toString(k));
    let keys = t => Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable.fromString(x), undefined)));
    let values = prim => prim.values();
    let entries = t => Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
      Stdlib_Option.getExn(Serializable.fromString(param[0]), undefined),
      param[1]
    ]));
    return {
      make: make,
      fromArray: fromArray,
      fromIterator: fromIterator,
      size: size,
      clear: clear,
      forEach: forEach,
      forEachWithKey: forEachWithKey,
      get: get,
      has: has,
      set: set,
      $$delete: $$delete,
      keys: keys,
      values: values,
      entries: entries
    };
  };
}

let Serializable$3 = Stdlib__Serializable.MakeTuple2(Stdlib__JSONSerializable.Int)(Stdlib__JSONSerializable.$$String);

function make$3(prim) {
  return new Map();
}

function fromArray$3(arr) {
  return new Map(arr.map(param => [
    Serializable$3.toString(param[0]),
    param[1]
  ]));
}

function fromIterator$3(iter) {
  return fromArray$3(Array.from(iter));
}

function size$3(prim) {
  return prim.size;
}

function clear$3(prim) {
  prim.clear();
}

function forEach$3(prim0, prim1) {
  prim0.forEach(prim1);
}

function forEachWithKey$3(t, f) {
  t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable$3.fromString(k), undefined)));
}

function get$3(t, k) {
  return t.get(Serializable$3.toString(k));
}

function has$3(t, k) {
  return t.has(Serializable$3.toString(k));
}

function set$3(t, k, v) {
  t.set(Serializable$3.toString(k), v);
}

function $$delete$3(t, k) {
  return t.delete(Serializable$3.toString(k));
}

function keys$3(t) {
  return Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable$3.fromString(x), undefined)));
}

function values$3(prim) {
  return prim.values();
}

function entries$3(t) {
  return Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
    Stdlib_Option.getExn(Serializable$3.fromString(param[0]), undefined),
    param[1]
  ]));
}

let IntString = {
  make: make$3,
  fromArray: fromArray$3,
  fromIterator: fromIterator$3,
  size: size$3,
  clear: clear$3,
  forEach: forEach$3,
  forEachWithKey: forEachWithKey$3,
  get: get$3,
  has: has$3,
  set: set$3,
  $$delete: $$delete$3,
  keys: keys$3,
  values: values$3,
  entries: entries$3
};

let Serializable$4 = Stdlib__Serializable.MakeTuple2(Stdlib__JSONSerializable.$$String)(Stdlib__JSONSerializable.Int);

function make$4(prim) {
  return new Map();
}

function fromArray$4(arr) {
  return new Map(arr.map(param => [
    Serializable$4.toString(param[0]),
    param[1]
  ]));
}

function fromIterator$4(iter) {
  return fromArray$4(Array.from(iter));
}

function size$4(prim) {
  return prim.size;
}

function clear$4(prim) {
  prim.clear();
}

function forEach$4(prim0, prim1) {
  prim0.forEach(prim1);
}

function forEachWithKey$4(t, f) {
  t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable$4.fromString(k), undefined)));
}

function get$4(t, k) {
  return t.get(Serializable$4.toString(k));
}

function has$4(t, k) {
  return t.has(Serializable$4.toString(k));
}

function set$4(t, k, v) {
  t.set(Serializable$4.toString(k), v);
}

function $$delete$4(t, k) {
  return t.delete(Serializable$4.toString(k));
}

function keys$4(t) {
  return Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable$4.fromString(x), undefined)));
}

function values$4(prim) {
  return prim.values();
}

function entries$4(t) {
  return Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
    Stdlib_Option.getExn(Serializable$4.fromString(param[0]), undefined),
    param[1]
  ]));
}

let StringInt = {
  make: make$4,
  fromArray: fromArray$4,
  fromIterator: fromIterator$4,
  size: size$4,
  clear: clear$4,
  forEach: forEach$4,
  forEachWithKey: forEachWithKey$4,
  get: get$4,
  has: has$4,
  set: set$4,
  $$delete: $$delete$4,
  keys: keys$4,
  values: values$4,
  entries: entries$4
};

let Serializable$5 = Stdlib__Serializable.MakeTuple2(Stdlib__JSONSerializable.Int)(Stdlib__JSONSerializable.Int);

function make$5(prim) {
  return new Map();
}

function fromArray$5(arr) {
  return new Map(arr.map(param => [
    Serializable$5.toString(param[0]),
    param[1]
  ]));
}

function fromIterator$5(iter) {
  return fromArray$5(Array.from(iter));
}

function size$5(prim) {
  return prim.size;
}

function clear$5(prim) {
  prim.clear();
}

function forEach$5(prim0, prim1) {
  prim0.forEach(prim1);
}

function forEachWithKey$5(t, f) {
  t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable$5.fromString(k), undefined)));
}

function get$5(t, k) {
  return t.get(Serializable$5.toString(k));
}

function has$5(t, k) {
  return t.has(Serializable$5.toString(k));
}

function set$5(t, k, v) {
  t.set(Serializable$5.toString(k), v);
}

function $$delete$5(t, k) {
  return t.delete(Serializable$5.toString(k));
}

function keys$5(t) {
  return Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable$5.fromString(x), undefined)));
}

function values$5(prim) {
  return prim.values();
}

function entries$5(t) {
  return Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
    Stdlib_Option.getExn(Serializable$5.fromString(param[0]), undefined),
    param[1]
  ]));
}

let IntInt = {
  make: make$5,
  fromArray: fromArray$5,
  fromIterator: fromIterator$5,
  size: size$5,
  clear: clear$5,
  forEach: forEach$5,
  forEachWithKey: forEachWithKey$5,
  get: get$5,
  has: has$5,
  set: set$5,
  $$delete: $$delete$5,
  keys: keys$5,
  values: values$5,
  entries: entries$5
};

let Serializable$6 = Stdlib__Serializable.MakeTuple2(Stdlib__JSONSerializable.Float)(Stdlib__JSONSerializable.Float);

function make$6(prim) {
  return new Map();
}

function fromArray$6(arr) {
  return new Map(arr.map(param => [
    Serializable$6.toString(param[0]),
    param[1]
  ]));
}

function fromIterator$6(iter) {
  return fromArray$6(Array.from(iter));
}

function size$6(prim) {
  return prim.size;
}

function clear$6(prim) {
  prim.clear();
}

function forEach$6(prim0, prim1) {
  prim0.forEach(prim1);
}

function forEachWithKey$6(t, f) {
  t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable$6.fromString(k), undefined)));
}

function get$6(t, k) {
  return t.get(Serializable$6.toString(k));
}

function has$6(t, k) {
  return t.has(Serializable$6.toString(k));
}

function set$6(t, k, v) {
  t.set(Serializable$6.toString(k), v);
}

function $$delete$6(t, k) {
  return t.delete(Serializable$6.toString(k));
}

function keys$6(t) {
  return Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable$6.fromString(x), undefined)));
}

function values$6(prim) {
  return prim.values();
}

function entries$6(t) {
  return Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
    Stdlib_Option.getExn(Serializable$6.fromString(param[0]), undefined),
    param[1]
  ]));
}

let FloatFloat = {
  make: make$6,
  fromArray: fromArray$6,
  fromIterator: fromIterator$6,
  size: size$6,
  clear: clear$6,
  forEach: forEach$6,
  forEachWithKey: forEachWithKey$6,
  get: get$6,
  has: has$6,
  set: set$6,
  $$delete: $$delete$6,
  keys: keys$6,
  values: values$6,
  entries: entries$6
};

let Tuple2 = {
  Make: Make$2,
  IntString: IntString,
  StringInt: StringInt,
  IntInt: IntInt,
  FloatFloat: FloatFloat
};

function Make$3(A) {
  return B => (C => {
    let Serializable = Stdlib__Serializable.MakeTuple3(A)(B)(C);
    let make = prim => new Map();
    let fromArray = arr => new Map(arr.map(param => [
      Serializable.toString(param[0]),
      param[1]
    ]));
    let fromIterator = iter => fromArray(Array.from(iter));
    let size = prim => prim.size;
    let clear = prim => {
      prim.clear();
    };
    let forEach = (prim0, prim1) => {
      prim0.forEach(prim1);
    };
    let forEachWithKey = (t, f) => {
      t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable.fromString(k), undefined)));
    };
    let get = (t, k) => t.get(Serializable.toString(k));
    let has = (t, k) => t.has(Serializable.toString(k));
    let set = (t, k, v) => {
      t.set(Serializable.toString(k), v);
    };
    let $$delete = (t, k) => t.delete(Serializable.toString(k));
    let keys = t => Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable.fromString(x), undefined)));
    let values = prim => prim.values();
    let entries = t => Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
      Stdlib_Option.getExn(Serializable.fromString(param[0]), undefined),
      param[1]
    ]));
    return {
      make: make,
      fromArray: fromArray,
      fromIterator: fromIterator,
      size: size,
      clear: clear,
      forEach: forEach,
      forEachWithKey: forEachWithKey,
      get: get,
      has: has,
      set: set,
      $$delete: $$delete,
      keys: keys,
      values: values,
      entries: entries
    };
  });
}

let IntIntInt = (C => {
  let Serializable = Stdlib__Serializable.MakeTuple3(Stdlib__JSONSerializable.Int)(Stdlib__JSONSerializable.Int)(C);
  let make = prim => new Map();
  let fromArray = arr => new Map(arr.map(param => [
    Serializable.toString(param[0]),
    param[1]
  ]));
  let fromIterator = iter => fromArray(Array.from(iter));
  let size = prim => prim.size;
  let clear = prim => {
    prim.clear();
  };
  let forEach = (prim0, prim1) => {
    prim0.forEach(prim1);
  };
  let forEachWithKey = (t, f) => {
    t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable.fromString(k), undefined)));
  };
  let get = (t, k) => t.get(Serializable.toString(k));
  let has = (t, k) => t.has(Serializable.toString(k));
  let set = (t, k, v) => {
    t.set(Serializable.toString(k), v);
  };
  let $$delete = (t, k) => t.delete(Serializable.toString(k));
  let keys = t => Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable.fromString(x), undefined)));
  let values = prim => prim.values();
  let entries = t => Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
    Stdlib_Option.getExn(Serializable.fromString(param[0]), undefined),
    param[1]
  ]));
  return {
    make: make,
    fromArray: fromArray,
    fromIterator: fromIterator,
    size: size,
    clear: clear,
    forEach: forEach,
    forEachWithKey: forEachWithKey,
    get: get,
    has: has,
    set: set,
    $$delete: $$delete,
    keys: keys,
    values: values,
    entries: entries
  };
})(Stdlib__JSONSerializable.Int);

let FloatFloatFloat = (C => {
  let Serializable = Stdlib__Serializable.MakeTuple3(Stdlib__JSONSerializable.Float)(Stdlib__JSONSerializable.Float)(C);
  let make = prim => new Map();
  let fromArray = arr => new Map(arr.map(param => [
    Serializable.toString(param[0]),
    param[1]
  ]));
  let fromIterator = iter => fromArray(Array.from(iter));
  let size = prim => prim.size;
  let clear = prim => {
    prim.clear();
  };
  let forEach = (prim0, prim1) => {
    prim0.forEach(prim1);
  };
  let forEachWithKey = (t, f) => {
    t.forEach((v, k) => f(v, Stdlib_Option.getExn(Serializable.fromString(k), undefined)));
  };
  let get = (t, k) => t.get(Serializable.toString(k));
  let has = (t, k) => t.has(Serializable.toString(k));
  let set = (t, k, v) => {
    t.set(Serializable.toString(k), v);
  };
  let $$delete = (t, k) => t.delete(Serializable.toString(k));
  let keys = t => Stdlib__Array.valuesIter(Array.from(t.keys()).map(x => Stdlib_Option.getExn(Serializable.fromString(x), undefined)));
  let values = prim => prim.values();
  let entries = t => Stdlib__Array.valuesIter(Array.from(t.entries()).map(param => [
    Stdlib_Option.getExn(Serializable.fromString(param[0]), undefined),
    param[1]
  ]));
  return {
    make: make,
    fromArray: fromArray,
    fromIterator: fromIterator,
    size: size,
    clear: clear,
    forEach: forEach,
    forEachWithKey: forEachWithKey,
    get: get,
    has: has,
    set: set,
    $$delete: $$delete,
    keys: keys,
    values: values,
    entries: entries
  };
})(Stdlib__JSONSerializable.Float);

let Tuple3 = {
  Make: Make$3,
  IntIntInt: IntIntInt,
  FloatFloatFloat: FloatFloatFloat
};

let Key = {
  $$Array: $$Array,
  Tuple2: Tuple2,
  Tuple3: Tuple3
};

let Serializable$7;

let JSONSerializable;

export {
  Serializable$7 as Serializable,
  JSONSerializable,
  Make,
  Key,
}
/* Serializable Not a pure module */
