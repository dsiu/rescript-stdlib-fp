// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_List from "@rescript/runtime/lib/es6/Stdlib_List.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as TableclothList from "./Tablecloth/TableclothList.res.mjs";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";

function listToOption(l) {
  if (l !== 0) {
    return Primitive_option.some(l.hd);
  }
  
}

function foldLeft0(xs, f) {
  let init = Stdlib_List.getExn(xs, 0);
  let rest = Stdlib_List.tailExn(xs);
  return Stdlib_List.reduce(rest, init, f);
}

function foldRight0(xs, f) {
  let end = Stdlib_List.length(xs) - 1 | 0;
  let init = Stdlib_List.getExn(xs, end);
  let rest = Stdlib_Option.getExn(Stdlib_List.take(xs, end), undefined);
  return Stdlib_List.reduceReverse(rest, init, f);
}

function transpose(_a) {
  while (true) {
    let a = _a;
    if (a === 0) {
      return /* [] */0;
    }
    let match = a.hd;
    if (match !== 0) {
      let match$1 = Stdlib_List.unzip(Stdlib_List.map(a.tl, y => [
        Stdlib_List.headExn(y),
        Stdlib_List.tailExn(y)
      ]));
      return {
        hd: {
          hd: match.hd,
          tl: match$1[0]
        },
        tl: transpose({
          hd: match.tl,
          tl: match$1[1]
        })
      };
    }
    _a = a.tl;
    continue;
  };
}

function combinationIf2(a, b, f) {
  return Stdlib_List.reduce(a, /* [] */0, (acc, x) => Stdlib_List.concat(acc, Stdlib_List.reduce(b, /* [] */0, (acc, y) => {
    let r = f(x, y);
    if (r !== undefined) {
      return Stdlib_List.concat(acc, {
        hd: Primitive_option.valFromOption(r),
        tl: /* [] */0
      });
    } else {
      return acc;
    }
  })));
}

function combination2(a, b, f) {
  return combinationIf2(a, b, (x, y) => Primitive_option.some(f(x, y)));
}

function unfold(p, g, b) {
  if (p(b)) {
    return /* [] */0;
  }
  let match = g(b);
  return {
    hd: match[0],
    tl: unfold(p, g, match[1])
  };
}

let L;

let zipWith = TableclothList.map2;

export {
  L,
  listToOption,
  foldLeft0,
  foldRight0,
  transpose,
  zipWith,
  combinationIf2,
  combination2,
  unfold,
}
/* TableclothList Not a pure module */
